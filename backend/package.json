{
  "name": "backend",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "generate-large-file": "base64 /dev/urandom | head -c 4G > public/large-files/4gb.txt",
    "docker-build": "docker build -t expserver-tester-image ./src",
    "create-uploads-folder": "node -e \"const fs = require('fs'); const path = require('path'); const folder = path.join(process.env.INIT_CWD || process.cwd(), 'uploads'); if (!fs.existsSync(folder)) { fs.mkdirSync(folder, { recursive: true }); console.log('✅ Created uploads folder'); } else { console.log('📁 uploads folder already exists — nothing to do.'); }\"",
    "setup": "npx sequelize-cli db:migrate && npm run create-uploads-folder",
    "reset": "npx sequelize-cli db:drop && npx sequelize-cli db:create && npx sequelize-cli db:migrate && npm run create-uploads-folder",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_OPTIONS=\"--max-old-space-size=4096\" nodemon src/index.ts",
    "build": "rm -rf ./dist && tsc",
    "start": "node --max-old-space-size=4096 dist/index.js",
    "precompile": "rm -rf ./compile && mkdir compile && npm run build",
    "compile": "npm run precompile && esbuild dist/index.js --bundle --outfile=compile/build.cjs --format=cjs --platform=node --external:*.node && cd compile && npx pkg build.cjs && npm run postcompile",
    "postcompile": "cd compile && rm -rf build.cjs",
    "generate-desc": "ts-node src/generate-description.ts"
  },
  "keywords": [],
  "author": "Th3BossC",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/dockerode": "^3.3.32",
    "@types/express": "^5.0.1",
    "@types/node": "^22.7.4",
    "@types/node-os-utils": "^1.3.4",
    "@types/redis": "^4.0.10",
    "@types/uuid": "^10.0.0",
    "chalk": "^5.3.0",
    "esbuild": "^0.24.0",
    "express": "^4.21.2",
    "nexe": "^4.0.0-rc.6",
    "nodemon": "^3.1.7",
    "pino": "^9.5.0",
    "pino-pretty": "^13.0.0",
    "pkg": "^5.8.1",
    "typescript": "^5.6.2",
    "winston": "^3.17.0"
  },
  "dependencies": {
    "@mikro-orm/core": "^6.4.13",
    "@mikro-orm/migrations": "^6.4.13",
    "@mikro-orm/postgresql": "^6.4.13",
    "@mikro-orm/reflection": "^6.4.13",
    "cors": "^2.8.5",
    "dockerode": "^4.0.2",
    "dotenv": "^16.5.0",
    "eventemitter3": "^5.0.1",
    "express-async-handler": "^1.2.0",
    "http-parser-js": "^0.5.9",
    "moment": "^2.30.1",
    "multer": "^1.4.5-lts.1",
    "node-os-utils": "^1.3.7",
    "pg": "^8.14.1",
    "redis": "^4.7.0",
    "sequelize": "^6.37.7",
    "sequelize-typescript": "^2.1.6",
    "socket.io": "^4.8.0",
    "socket.io-redis": "^5.4.0",
    "ts-node": "^10.9.2",
    "uuid": "^11.1.0"
  }
}
